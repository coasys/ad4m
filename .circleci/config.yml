version: 2.1
references:
  ad4m_image: &ad4m_image coasys/ad4m-ci-linux:latest@sha256:16a04358abc4b12ea4cf3a231cfdfea38ac6ae0cc4f937f6c0ed5d98dfb574e7

# Self-hosted machine runner on marvin (AMD Ryzen 9 9950X, 60GB RAM, Ubuntu 25.04)
# Resource class: coasys/marvin

commands:
  fix_workdir:
    description: "Recover from corrupted .git directory (e.g. after disk-full)"
    steps:
      - run:
          name: Fix corrupted working directory
          command: |
            if [ -d .git ] && ! git rev-parse --git-dir >/dev/null 2>&1; then
              echo "Corrupted .git directory detected, removing for fresh clone..."
              rm -rf .git
            fi

  setup_env:
    description: "Set up build environment (Node, Rust, Deno, Go, pnpm)"
    parameters:
      node_version:
        type: string
        default: "24"
    steps:
      - run:
          name: Setup build environment
          command: |
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" --no-use
            NODE_PATH=$(nvm which << parameters.node_version >> 2>/dev/null || true)
            if [ -z "$NODE_PATH" ] || [ ! -x "$NODE_PATH" ]; then
              echo "Node << parameters.node_version >> not found, installing via nvm..."
              nvm install << parameters.node_version >>
              NODE_PATH=$(nvm which << parameters.node_version >>)
            fi
            NODE_BIN_DIR=$(dirname "$NODE_PATH")
            echo "export PATH=\"$NODE_BIN_DIR:$HOME/.cargo/bin:$HOME/.deno/bin:$HOME/.local/go/bin:$HOME/.npm-global/bin:$HOME/.local/bin:$HOME/go/bin:$PATH\"" >> $BASH_ENV
            source $BASH_ENV
            echo "Node: $(node --version)"
            echo "npm: $(npm --version)"
            echo "Rust: $(rustc --version)"
            echo "Installing wasm-bindgen-cli for Rust WASM builds..."
            cargo install wasm-bindgen-cli --version 0.2.118
            echo "Deno: $(deno --version | head -1)"
            echo "Go: $(go version)"
            echo "pnpm: $(pnpm --version || echo 'not installed yet')"

  setup_integration_test_environment:
    description: "Setup common integration test environment"
    steps:
      - fix_workdir
      - checkout
      - run:
          name: Restore binaries from local stash (no CircleCI network storage needed — same host)
          command: |
            # CI_ARTIFACT_DIR: env-driven so the shared self-hosted runner
            # can be re-imaged / re-owned without editing this file. Default
            # is a system-level path that does not couple this config to any
            # particular user account on the runner host. The runner must
            # `mkdir -p /var/lib/ci-artifacts && chown <ci-user> /var/lib/ci-artifacts`
            # once at install time. Long-term this should move to GitLab CI
            # (Nico's 2026-08-27 review); the /var/lib default is the short-term
            # de-marvin-ification step.
            CI_ARTIFACT_DIR="${CI_ARTIFACT_DIR:-/var/lib/ci-artifacts}"
            mkdir -p target/release
            # Remove before copy: unlinking avoids ETXTBSY if a previous executor still holds the binary
            rm -f target/release/ad4m target/release/ad4m-executor
            cp "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}/ad4m" target/release/ad4m
            cp "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}/ad4m-executor" target/release/ad4m-executor
      - setup_env
      - run:
          name: Install dependencies
          command: pnpm install --no-frozen-lockfile
      - run:
          name: Install core dependencies
          command: cd ./core && pnpm install --no-frozen-lockfile
      - run:
          name: Build core package
          command: cd ./core && pnpm run build
      - run:
          name: Clear stale bootstrap-language build cache (cross-branch shared workdir)
          command: |
            rm -rf .turbo bootstrap-languages/*/.turbo bootstrap-languages/*/build
      - run:
          # Isolate `hc` AND `kitsune2-bootstrap-srv` per CI run so the host's
          # global copies (shared across branches / other PRs on this
          # self-hosted runner) don't drift out of sync with Cargo.lock.
          # The workspace's Cargo.lock is the source of truth; this script
          # installs matching `hc` (from coasys/holochain rev) and
          # `kitsune2-bootstrap-srv` (from crates.io version) into
          # $REPO/.hc-toolchain/bin/. Consumers: prepareTestDirectory.sh,
          # bootstrap-language build.sh files, tests/js/utils/utils.ts.
          # Fixes: `Failed to unpack hApp: unknown field 'signal_url'`
          # (hc mismatch) and multi-user signal-routing timeouts
          # (kitsune2-bootstrap-srv wire-protocol drift).
          name: Install pinned Holochain + kitsune2 CLIs (matches Cargo.lock)
          command: bash scripts/install-hc-toolchain.sh
          no_output_timeout: 30m
      - run:
          name: Build bootstrap languages
          command: pnpm run build-languages

jobs:
  build-and-test:
    machine: true
    resource_class: coasys/marvin
    steps:
      - fix_workdir
      - checkout
      - setup_env
      - run:
          name: Clean up stale local artifact stashes (older than 1 day)
          command: |
            CI_ARTIFACT_DIR="${CI_ARTIFACT_DIR:-/var/lib/ci-artifacts}"
            find "$CI_ARTIFACT_DIR" -maxdepth 1 -mindepth 1 -mtime +1 -exec rm -rf {} + 2>/dev/null || true
      - run:
          # Install per-workspace `hc` + `kitsune2-bootstrap-srv` matching
          # Cargo.lock. Needed here (in build-and-test) because pnpm
          # build-libs -> the bootstrap-languages build.sh files pack DNAs
          # with `hc`, and downstream integration tests spawn
          # `kitsune2-bootstrap-srv`. Both must match what the executor is
          # linked against, otherwise packed bundles trip `unknown field
          # 'signal_url'` at install time and cross-node signals never
          # route between agents.
          name: Install pinned Holochain + kitsune2 CLIs (matches Cargo.lock)
          command: bash scripts/install-hc-toolchain.sh
          no_output_timeout: 30m
      - run:
          name: cargo fmt --check
          command: cargo fmt --check
      - run:
          name: Install dependencies
          command: pnpm install --no-frozen-lockfile
      - run:
          name: Build dapp
          command: pnpm build-dapp
      - run:
          name: Install core dependencies
          command: cd ./core && pnpm install --no-frozen-lockfile
      - run:
          name: Build deno snapshot
          command: pnpm run build-deno-snapshot
          no_output_timeout: 30m
      - run:
          name: Remove stale binaries (self-hosted runner incremental cache)
          command: rm -f target/release/ad4m target/release/ad4m-executor
      - run:
          name: Build ADAM (without bootstrap languages)
          command: pnpm run build-libs
          no_output_timeout: 30m
      - run:
          name: cargo clippy
          no_output_timeout: 30m
          command: cargo clippy --all --exclude ad4m-launcher
      - run:
          name: Stash binaries locally (skip CircleCI network storage — all jobs run on same host)
          command: |
            CI_ARTIFACT_DIR="${CI_ARTIFACT_DIR:-/var/lib/ci-artifacts}"
            mkdir -p "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}"
            cp target/release/ad4m target/release/ad4m-executor "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}/"
      - run:
          name: Remove pnpm patches
          command: node removePnpm.js
      - run:
          name: Install dependencies
          command: pnpm install --no-frozen-lockfile
      - run:
          name: Install core dependencies
          command: cd ./core && pnpm install --no-frozen-lockfile
      - run:
          name: Root tests
          command: pnpm test
          no_output_timeout: 30m

  integration-tests-js:
    machine: true
    resource_class: coasys/marvin
    steps:
      - setup_integration_test_environment
      - run:
          name: Run integration tests
          command: |
            mkdir -p /tmp/test-artifacts
            set -o pipefail
            cd ./tests/js && pnpm run test-main 2>&1 | tee /tmp/test-artifacts/mocha.log
          no_output_timeout: 30m
      - run:
          name: Collect logs on failure
          when: always
          command: |
            mkdir -p /tmp/test-artifacts
            shopt -s nullglob
            for ad4m_dir in tests/js/tst-tmp/agents/*/ad4m; do
              agent_id="$(basename "$(dirname "$ad4m_dir")")"
              mkdir -p "/tmp/test-artifacts/$agent_id"
              cp -r "$ad4m_dir/logs" "/tmp/test-artifacts/$agent_id/" 2>/dev/null || true
              cp -r "$ad4m_dir/holochain" "/tmp/test-artifacts/$agent_id/" 2>/dev/null || true
            done
            # Capture port state for debugging
            lsof -i -P -n 2>/dev/null | grep LISTEN > /tmp/test-artifacts/listening-ports.txt || true
      - store_artifacts:
          when: always
          path: /tmp/test-artifacts
          destination: test-logs

  integration-tests-multi-user-simple:
    machine: true
    resource_class: coasys/marvin
    steps:
      - setup_integration_test_environment
      - run:
          name: Run multi-user-simple integration tests
          command: cd ./tests/js && ./test-multi-user-with-setup.sh
          no_output_timeout: 30m

  integration-tests-email-verification:
    machine: true
    resource_class: coasys/marvin
    steps:
      - setup_integration_test_environment
      - run:
          name: Run email-verification integration tests
          command: cd ./tests/js && ./email-verification-test-with-setup.sh
          no_output_timeout: 30m

  integration-tests-mcp:
    machine: true
    resource_class: coasys/marvin
    steps:
      - setup_integration_test_environment
      - run:
          name: Run MCP integration tests
          command: cd ./tests/js && pnpm install && pnpm run test-mcp
          no_output_timeout: 30m

  integration-tests-model:
    machine: true
    resource_class: coasys/marvin
    steps:
      - setup_integration_test_environment
      - run:
          name: Run Ad4mModel unit + integration tests
          command: cd ./tests/js && pnpm run test-model
          no_output_timeout: 30m

  # Runs everything gated by #[ignore = "llm-e2e"] (Rust) or LLM_E2E=1 (JS).
  # These hit Marvin's local Ollama; too slow + occasionally flaky for
  # per-push CI, so they run once nightly against `dev`. Trigger:
  # `workflows.nightly-llm-e2e` at the bottom of this file.
  llm-e2e:
    machine: true
    resource_class: coasys/marvin
    steps:
      # Skip-if-unchanged: only run the expensive suite when dev has moved
      # since the last successful nightly. `circleci-agent step halt` ends
      # the job cleanly (green) so the workflow shows no wasted GPU cycles
      # on quiet days. `CIRCLECI_API_TOKEN` comes from the `nightly-llm-e2e`
      # context — set once via the API, org-scoped, read-only calls only.
      - checkout
      - run:
          name: Skip if dev SHA unchanged since last successful llm-e2e
          command: |
            LAST_SHA=$(curl -sL -H "Circle-Token: $CIRCLECI_API_TOKEN" \
              "https://circleci.com/api/v2/insights/gh/coasys/ad4m/workflows/nightly-llm-e2e/jobs?branch=dev" \
              | jq -r '.items[]? | select(.status=="success") | .commit_id' \
              | head -1)
            if [ -n "$LAST_SHA" ] && [ "$LAST_SHA" = "$CIRCLE_SHA1" ]; then
              echo "dev @ $CIRCLE_SHA1 already tested by last nightly — halting"
              circleci-agent step halt
            fi
            if [ -z "$LAST_SHA" ]; then
              echo "no previous successful llm-e2e run found — proceeding"
            else
              echo "dev @ $CIRCLE_SHA1, last tested $LAST_SHA — proceeding"
            fi
      - setup_integration_test_environment
      - run:
          name: Rust LLM-E2E (interpretation + harness suites)
          command: |
            cargo test --release --lib \
              perspectives::interpretation_e2e \
              perspectives::interpretation_harness_e2e \
              -- --ignored --test-threads=1 --nocapture
          no_output_timeout: 60m
      - run:
          name: JS LLM-E2E (auto-processor + interpretation suites)
          command: |
            cd ./tests/js && LLM_E2E=1 pnpm run test-model
          no_output_timeout: 60m
      - run:
          name: JS LLM-E2E (integration — includes auto-processor-neighbourhood)
          command: |
            cd ./tests/js && LLM_E2E=1 pnpm run test-integration
          no_output_timeout: 60m

  integration-tests-cli:
    machine: true
    resource_class: coasys/marvin
    steps:
      - fix_workdir
      - checkout
      - run:
          name: Restore binaries from local stash
          command: |
            CI_ARTIFACT_DIR="${CI_ARTIFACT_DIR:-/var/lib/ci-artifacts}"
            mkdir -p target/release
            cp "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}/ad4m" target/release/ad4m
            cp "$CI_ARTIFACT_DIR/${CIRCLE_SHA1}/ad4m-executor" target/release/ad4m-executor
      - setup_env
      - run:
          name: git submodule
          command: git submodule update --init --recursive
      - run:
          name: Run binaries test script
          command: ./tests/bats/bin/bats tests/binaries.bats
      - run:
          name: Run integration test script
          command: ./tests/bats/bin/bats tests/integration.bats

  plugin-ad4m-tests:
    machine: true
    resource_class: coasys/marvin
    steps:
      - fix_workdir
      - checkout
      - run:
          name: Install dependencies
          working_directory: plugins/ad4m
          command: npm install
      - run:
          name: Run tests
          working_directory: plugins/ad4m
          command: npm test

workflows:
  version: 2
  build-and-test:
    jobs:
      - build-and-test
      - plugin-ad4m-tests
      - integration-tests-js:
          requires:
            - build-and-test
      - integration-tests-multi-user-simple:
          requires:
            - build-and-test
      - integration-tests-email-verification:
          requires:
            - build-and-test
      - integration-tests-mcp:
          requires:
            - build-and-test
      - integration-tests-model:
          requires:
            - build-and-test

  # Nightly at 03:00 UTC against dev. Runs the LLM-heavy suites that are
  # skipped from the per-push workflow (Rust `#[ignore = "llm-e2e"]` +
  # JS `LLM_E2E=1`). Same resource class (Marvin) so the Ollama endpoint
  # is local. If it fails, look for a real regression against dev — the
  # gate exists to keep per-push CI fast, not to hide breakage.
  nightly-llm-e2e:
    triggers:
      - schedule:
          cron: "0 3 * * *"
          filters:
            branches:
              only:
                - dev
    jobs:
      - llm-e2e:
          # Provides `CIRCLECI_API_TOKEN` for the skip-if-unchanged preflight.
          context:
            - nightly-llm-e2e

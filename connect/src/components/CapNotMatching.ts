import { html } from "lit";

export default function CapNotMatched() {
  return html`
    <div>
      <h1 class="heading">Capabilities not matched</h1>
      <p class="body">Try again</p>
    </div>
  `;
}

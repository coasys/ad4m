// Legacy migration module (Rusqlite → SPARQL).
// This module is retained as a no-op stub for backwards compatibility
// so that existing `mod migration;` declarations still compile.
// It can be fully deleted in a future cleanup pass.

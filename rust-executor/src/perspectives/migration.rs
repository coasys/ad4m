// Legacy migration module (Rusqlite → SurrealDB).
// SurrealDB has been removed; this module is retained as a no-op stub
// so that existing `mod migration;` declarations still compile.
// It can be fully deleted in a future cleanup pass.

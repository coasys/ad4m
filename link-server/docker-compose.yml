services:
  link-server:
    build: .
    ports:
      - "${PORT:-3456}:${PORT:-3456}"
    volumes:
      - link-server-data:/data
    environment:
      PORT: "${PORT:-3456}"
      DATA_DIR: /data
      AUTO_ADMIT: "${AUTO_ADMIT:-false}"
    restart: unless-stopped

volumes:
  link-server-data:
